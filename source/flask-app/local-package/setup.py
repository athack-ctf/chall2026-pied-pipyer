from setuptools import setup, find_packages

setup(
    name='hello-world-package',
    version='1.0.0',
    description='Hello World package for CI Testing',
    packages=find_packages(),
    python_requires='>=3.8',
)
